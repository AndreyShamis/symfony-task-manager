{% extends 'base.html.twig' %}

{% block body %}
    <h1>Skipmode</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ skipMode.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ skipMode.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('skipmode_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('skipmode_edit', { 'id': skipMode.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
