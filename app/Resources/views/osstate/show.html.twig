{% extends 'base.html.twig' %}

{% block body %}
    <h1>Osstate</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ osState.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ osState.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('osstate_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('osstate_edit', { 'id': osState.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
